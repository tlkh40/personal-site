---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { Icon } from 'astro-icon';
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<main class="max-h-[100svh] overflow-y-scroll overflow-x-hidden snap snap-y snap-mandatory">
			<div class="snap-start">
				<Header />
				<section transition:animate="slide" class="h-[calc(100svh-var(--header))] flex items-center">
					<div class="px-3 container mx-auto">
						<h2 class="font-bold text-5xl mb-2">Hey! 👋</h2>
						<p class="text-xl md:w-2/5 tracking-wide">
							I'm Bryan, but I'm usually known as "tlkh40". I'm a software developer
							mainly focusing on frontend development.
						</p>
					</div>
				</section>
			</div>
			<div class="snap-start h-[100svh]">
				<section transition:animate="slide" class="h-[100svh] flex items-center justify-end bg-gradient-to-b from-[--bg] to-[--secondary] relative isolate overflow-hidden">
					<div class="absolute inset-0 bg-gray-800 z-[-1] bg-opacity-70"></div>
					<div class="ducks">
						<Icon name="noto:duck" class="duck" />
						<Icon name="noto:baby-chick" class="duck" />
						<Icon name="noto:cat" class="duck" />
					</div>
					<div class="px-3 container mx-auto">
						<h2 class="font-bold text-5xl mb-2">About me</h2>
						<p class="text-xl tracking-wide">
							In 2021 I started programming when a friend invited me to make
							an image hosting service with him. While we did garner some users,
							the service itself wasn't great. After that project, I started
							doing react to modify the pterodactyl panel. Since then I've worked
							and lead the development of some projects such as
							<a href="https://staging-docs.berrybyte.net/lime/overview" class="underline underline-offset-2 decoration-[--primary] decoration-1 hover:decoration-2 font-semibold">
								Berrybyte's lime</a>!
						</p>
					</div>
				</section>
			</div>
			<div class="snap-start bg-[--secondary]">
				<section class="h-[calc(100svh-var(--footer))] flex justify-center items-center text-center">
					<span class="text-4xl">"👌👌✌✌😁" - 2023</span>
				</section>
				<Footer />
			</div>
		</main>
	</body>
</html>


<style>
	.duck {
		z-index: -2;
		animation: moveleduck 10s linear infinite;
		position: absolute;
		height: 8rem;
		width: 8rem;
		translate: var(--xb) var(--yb);
	}
	
	.duck:nth-child(1) {
		--xa: 100vw;
		--xb: -100vw;
		--ya: -75vh;
		--yb: 110vh;
		--rotation: 720deg;
	}

	.duck:nth-child(2) {
		--xa: -10vw;
		--xb: 75vw;
		--ya: 75vh;
		--yb: -110vh;
		--rotation: 720deg;
	}

	.duck:nth-child(3) {
		--xa: 100vw;
		--xb: -100vw;
		--ya: 25vh;
		--yb: 0vh;
		--rotation: 720deg;
	}

	@keyframes moveleduck {
		0% {
			rotate: 0deg;
			/* transform: translateX(var(--x-b)) translateY(var(--y-b)); */
			translate: var(--xb) var(--yb);
		}
		100% {
			rotate: var(--rotation);
			/* transform: translateX(var(--x-b)) translateY(var(--y-b)); */
			translate: var(--xa) var(--ya);
		}   
	}
</style>